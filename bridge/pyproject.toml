[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cliproxy-usage-bridge"
version = "0.1.0"
description = "Local-only CLIProxyAPI quota bridge"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
